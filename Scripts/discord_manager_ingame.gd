extends Node

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	#this shit make no sense it just dont work more than once idk y
	
	# var owner_name = $"..".name
	# DiscordRPCLoader.find_place(owner_name)
	# DiscordRPC.refresh()
	pass
